
services:
  some-service:
    environments:
      prod:
        url: localhost:8080
        paths:
          /hello:
            methods:
              GET:
                description: calls the service to see if it's ready, then greets you
                input: void
      dev:
        extends: prod
        url: localhost:8080
        paths:
          /hello:
            methods:
              GET:
                description: a new hello version that greets you in your local language
